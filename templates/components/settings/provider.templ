package settings

// Simple provider fields component
templ GoogleProviderFields(settings *LLMSettings) {
    <div>Google Provider Fields</div>
}

templ GenericProviderFields(settings *LLMSettings) {
    <div>Generic Provider Fields</div>
}
